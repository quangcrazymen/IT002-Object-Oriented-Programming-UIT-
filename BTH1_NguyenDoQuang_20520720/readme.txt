Lưu ý file Makefile dùng để build project.
Thầy cd vào thư mục của project(có chứa Makefile) và gõ lệnh make test là project sẽ chạy.
(Do máy em chạy linux nên không dùng visual studio được ạ)
(sử dụng Compliler gcc 9.4.0 hoặc compliler tương tự compile được code C++17)
